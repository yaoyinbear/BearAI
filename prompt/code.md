## Spec/Plan阶段

``` text
所有留到后续要做的功能，请生成一份文档放到 todos 里去，并且按重要程度排序，最为重要的部分特殊标记。
```

``` text
生成票据，有以下需求：
- 实施阶段使用主代理控制 + 子代理具体实施的方式进行。
- 每个票据用子代理使用 implement 技能执行，各自使用全新的上下文。
- 每个票据完成后更新 checklist 、记录结果、问题、改票据状态为 completed。
- 票据出现错误尽可能在票据内修复，如果有问题无法修复需要中断整个执行过程，向我提供信息由我决断。
- 所以涉及到的文件路径使用相对路径。
- 实施票据子代理使用 gpt-5.6-terra 模型的高推理强度执行。
- 验收票据子代理使用 gpt-5.6-sol 模型的高推理强度执行。
```

## 实施阶段

``` text
我需要一条可以复制粘贴的指令：
- 使用主代理控制 + 子代理具体实施的方式进行票据的实施和验收。
- 顺序做完所有票据，每个票据用子代理使用 implement 技能执行，各自使用全新的上下文。
- 每个票据完成后更新 checklist 、记录结果、问题、改票据状态为 completed。
- 票据出现错误尽可能在票据内修复，如果有问题无法修复需要中断整个执行过程，向我提供信息由我决断。
- 所以涉及到的文件路径使用相对路径。
- 实施票据子代理使用 gpt-5.6-terra 模型的高推理强度执行。
- 验收票据子代理使用 gpt-5.6-sol 模型的高推理强度执行。
不要执行，给我完整的指令 
```

``` text
给我在新的上下文中可以直接粘贴的实施指令和审查指令
```

